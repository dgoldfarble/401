export VERILATOR_ROOT=/usr/share/verilator  #ADD VERILATOR PATH HERE

cd sim_src

#$VERILATOR_ROOT/bin/
verilator --cc MIPS.v --exe sim_main.cpp

cd obj_dir

make -j -f VMIPS.mk VMIPS

cp VMIPS ../../
